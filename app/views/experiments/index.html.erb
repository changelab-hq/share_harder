<h1>Experiments <%= link_to 'New Experiment', new_experiment_path, class: 'btn btn-outline-success float-right' %></h1>
<% @experiments.each do |e| %>
  <p><%= link_to e.name, edit_experiment_path(e) %></p>
<% end %>